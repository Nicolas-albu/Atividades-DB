-- Selecione todos os cargos ocupados pelos funcionários. Exiba apenas uma
-- ocorrência para cada cargo. Exiba a coluna com o nome COD_CARGO.

SELECT DISTINCT FUNC.NR_CARGO AS FUNC.COD_CARGO
FROM FUNC